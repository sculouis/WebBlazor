@inherits LayoutComponentBase

<body>
    <a class="back-to-top" href="#syi"><i class="glyphicon glyphicon-menu-up"></i></a>
    <div class="index-content">
        <div class="index-nav">
            <div class="nav-logo-box">
                <a href="">
                    <div class="index-logo-icon"></div>
                    <h1 class="index-logo-title">OA表單管理系統</h1>
                </a>
            </div>

            <div class="nav-info-box">
                <p><span class="glyphicon glyphicon-user nav-icon-size-peo"></span></p>
                <p class="sign-out"><a href=""><span class="glyphicon glyphicon-cog nav-icon-size-time"></span>管理中心</a></p>
                <p></p>
            </div>
        </div>
        <div class="main-right-box main-right-box-w100">
            <div class="main-content-box">
                <div class="main-breadcrumb-box">
                    <ol class="breadcrumb">
                        <!-- @{
                            if (ViewBag.Breadcrumb != null && ViewBag.Breadcrumb.Count > 0)
                            {
                                foreach (var item in (Dictionary<string, string>)ViewBag.Breadcrumb)
                                {
                                    if (!string.IsNullOrWhiteSpace(item.Value))
                                    {
                                        <li><a href="@item.Value">@item.Key</a></li>
                                    }
                                    else
                                    {
                                        <li class="active">@item.Key</li>
                                    }
                                }
                            }
                        } -->
                    </ol>
                </div>
                @Body
            </div>
        </div>
        <footer></footer>
    </div>
</body>
